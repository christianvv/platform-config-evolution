# EXAMPLE ONLY — do not apply this file directly with real values committed.
# Create these Secrets imperatively AFTER `helm install`, into the same namespace, e.g.:
#   kubectl create secret generic ldap-admin-secret \
#     --namespace devops-tools \
#     --from-literal=admin-password='your-password-here'
#   kubectl create secret generic gitlab-runner-secret \
#     --namespace devops-tools \
#     --from-literal=registration-token='your-token-here'
---
apiVersion: v1
kind: Secret
metadata:
  name: ldap-admin-secret
  namespace: devops-tools
type: Opaque
stringData:
  admin-password: "REPLACE_ME"
---
apiVersion: v1
kind: Secret
metadata:
  name: gitlab-runner-secret
  namespace: devops-tools
type: Opaque
stringData:
  registration-token: "REPLACE_ME"
